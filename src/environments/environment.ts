// Utilise par `ng serve` (configuration "development"). Remplace par
// environment.prod.ts au build production via fileReplacements (angular.json).
export const environment = {
	production: false,
	backendUrl: "http://localhost:3050",
};
